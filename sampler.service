[Unit]
Description=Tracker Box Sampler (433 MHz RF button trigger)
After=sound.target
Wants=sound.target

[Service]
Type=simple
ExecStart=/usr/local/bin/sampler --box b
Restart=always
RestartSec=2

[Install]
WantedBy=multi-user.target